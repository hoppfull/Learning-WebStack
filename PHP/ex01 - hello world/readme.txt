Start a local php server in this directory with the command: "php -localhost:8000"
The last number (8000) can apparently be almost any number
Then simply type "localhost:8000" in web browser to view yer page